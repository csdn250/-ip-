# 网络配置、UDP 发现与端口配置说明

## 1. 当前配置项

单片机网络配置现在包括：

```text
IP      = 192.168.1.30
MASK    = 255.255.255.0
GATEWAY = 192.168.1.1
TCP     = 8080
UDP     = 9999
```

其中 `UDP=9999` 是设备发现端口，固定用于上位机广播搜索；`TCP` 是业务通信端口，可以通过上位机命令修改并保存到 Flash。

## 2. 为什么要支持修改 TCP 端口

多个单片机如果 IP 不同，即使都监听 8080，上位机也可以同时连接它们，因为 TCP 连接由：

```text
本机IP:本机端口 + 对端IP:对端端口
```

共同确定。

但是支持修改端口仍然有意义：

- 方便现场区分不同设备或不同服务。
- 方便做路由器端口映射。
- 方便上位机按设备类型或用途建立不同连接策略。
- 避免后续扩展多个 TCP 服务时端口固定带来的限制。

## 3. UDP 发现回复

上位机发送：

```text
DISCOVER_DEVICE
```

单片机回复：

```text
ACK,DEVICE=STM32H743_RAW_TCP_ADC,IP=192.168.1.30,MASK=255.255.255.0,GW=192.168.1.1,MAC=B8:AE:1D:00:04:00,TCP=8080,UDP=9999\r\n
```

上位机应解析 `IP` 和 `TCP` 字段，然后连接：

```text
<IP>:<TCP>
```

## 4. 查询配置

发送：

```text
GET_NET\r\n
```

返回：

```text
NET,IP=192.168.1.30,MASK=255.255.255.0,GW=192.168.1.1,TCP=8080\r\n
```

## 5. 修改 IP 和端口

推荐命令：

```text
SET_NET,IP=192.168.1.50,MASK=255.255.255.0,GW=192.168.1.1,PORT=8081\r\n
```

兼容写法：

```text
SET_NET,IP=192.168.1.50,MASK=255.255.255.0,GW=192.168.1.1,TCP=8081\r\n
```

写入成功后返回：

```text
OK,NET_SAVED,REBOOTING\r\n
```

随后单片机会自动复位。复位后，上位机需要连接：

```text
192.168.1.50:8081
```

如果 `SET_NET` 命令不带 `PORT=` 或 `TCP=` 字段，则保持当前 TCP 端口不变。

## 6. Flash 保存记录

网络配置保存记录包含：

```text
magic
version
ip
netmask
gateway
tcp_port
crc
reserved
```

固件通过 `magic + version + crc` 判断 Flash 配置是否有效。当前版本号为 `2`，用于区分新增端口字段后的配置格式。
